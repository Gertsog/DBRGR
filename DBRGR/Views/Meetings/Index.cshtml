@model IEnumerable<DBRGR.Models.Meeting>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MeetDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MeetTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Candidate.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Client.ClientName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Payment.Seller)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MeetDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MeetTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Candidate.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Client.ClientName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Payment.Seller)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InvitationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.InvitationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InvitationID })
        </td>
    </tr>
}

</table>
